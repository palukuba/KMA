{
  "name": "dr-kambale-mondo-site",
  "version": "4.0.0",
  "description": "Site professionnel du Dr. KAMBALE MONDO - Architecture modulaire",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:preview": "npm run build && wrangler pages deploy dist --branch preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "terser": "^5.44.1",
    "vite": "^5.0.8",
    "wrangler": "^3.91.0"
  },
  "dependencies": {
    "alpinejs": "^3.13.3"
  }
}
